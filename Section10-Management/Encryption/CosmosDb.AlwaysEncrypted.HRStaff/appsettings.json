{
    // Application connects using the master key
    "CosmosEndpoint": "[ENDPOINT]",
    "CosmosMasterKey": "[MASTER-KEY]",

    // Encrypt and decrypt properties using CMKs stored in Azure Key Vault accessible via the Entra ID tenant ID, and the HRStaff application's client ID and client secret
    "TenantId": "[TENANT-ID]",
    "ClientId": "[CLIENT-ID]",
    "ClientSecret": "[CLIENT-SECRET]"
}
